package android.widget

import android.content.Context

/** JVM stub for android.widget.Button. */
open class Button(context: Context = Context()) {
    var isEnabled: Boolean = true
    var text: CharSequence? = null
}
